// React is a js library for building user interfaces.
// Component based approach/Architecture
// declarative approach
// DOM updates are handled gracefully
// Code Reusability
// It is designed for speed, speed of implementing the application simplicity and scalability.

// -----------------installation------------------- 
// npm i -g create-react-app
// create-react-app --version
// create-react-app <project name>

// Project name must be in small letters with no space between

// ----------------folder structure----------------------------------------
// package.json --> gives the metadata of our app (gives information about name of application, author, dependencies, versions)
// index.js --> contains whole code of react
// manifest.json--> contains metadata of our application
// favicon.ico --> the iocn that displays in the tab 
// -----------------------------------------------------
// npm start -->to run the react app
// ------------------------------------------------------
// babel --> A js compiler that converts the modern JS code/Program into browser compatible code that browsers can understand and run.
//           Because JS is evolving day by day like ES6,7,8,9 .So babel converts the code written in these versions into ES5 standard version.
//           like import export functionality into require(). let and const into var.
//           when we import "react" babel and web pack is automatically imported.


// web pack
// jsx--> JavaScript extension or XML.

// ReactDOM.render(<h1>Hello World</h1>,
// document.getElementById("root"))

// ReactDOM.render(React.createElement("h1",null,"Hello world"),
// document.getElementById("root"))

// both are the same code
//  <h1>Hello world</h1> is a JSX expression.